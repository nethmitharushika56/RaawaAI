# Backend environment example
OPENAI_API_KEY=
AWS_REGION=ap-south-1
DYNAMODB_TABLE=raawa-simulations
# If running a local DynamoDB instance set this to http://dynamodb:8000 or http://localhost:8000
DYNAMODB_ENDPOINT=
# AWS credentials (only required when using real AWS DynamoDB)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Optional keys
HF_API_KEY=
